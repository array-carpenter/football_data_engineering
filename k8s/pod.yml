apiVersion: v1
kind: Pod
metadata:
    name: nfl-pipeline
spec:
    restartPolicy: Never
    containers:
      - name: pipeline
        image: nfl-pipeline:latest
        imagePullPolicy: Never
        command: ["bash", "-c"]
        args:
          - "python download.py && cd dbt && dbt run && dbt test"